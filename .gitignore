# Python bytecode (if anyone runs the transpiled code directly)
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local probes
_*.capa

# NOTE: vendor/ is committed on purpose (self-contained path
# dependencies), and out/ + sbom/ are committed as the sample
# reports / audited-project SBOMs / capability SBOMs the README references.
# Regenerate them with ./generate.sh.
