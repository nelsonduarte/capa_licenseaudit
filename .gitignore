# Python bytecode (if anyone runs the transpiled code directly)
__pycache__/
*.pyc

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Local probes
_*.capa

# Package manager: git deps fetched + verified by `capa install`.
# vendor/ is materialised locally (not committed); capa.lock IS committed.
vendor/

# NOTE: out/ + sbom/ are committed as the sample reports / audited-project
# SBOMs / capability SBOMs the README references. Regenerate them with
# ./generate.sh.
